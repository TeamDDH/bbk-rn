export class Article {
  constructor({ id, title, desc, timeUpdated }) {
    this.id = id
    this.title = title
    this.desc = desc
    this.timeUpdated = timeUpdated
  }
}

export class Topic {
  constructor({ id, title, desc, timeUpdated }) {
    this.id = id
    this.title = title
    this.desc = desc
    this.timeUpdated = timeUpdated
  }
}
